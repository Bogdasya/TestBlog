<div class="errors">
  <span> You have <%= pluralize(@user.errors.count, "error") %></span>
  <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
  </ul>
</div>