<%= form_for user do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :surname %>
  <%= f.text_field :surname %>

  <%= f.filepicker_field :image %>

  <%= f.submit  t('update_profile'), class: "btn btn-large btn-primary", id: "update_user" %>
<% end %>